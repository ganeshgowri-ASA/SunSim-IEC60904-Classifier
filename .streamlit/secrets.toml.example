# Streamlit Cloud Secrets Configuration
# Copy this file to secrets.toml and fill in your Railway PostgreSQL credentials
# DO NOT commit secrets.toml to version control!

# Railway PostgreSQL Database Connection
# Get these values from Railway Dashboard > PostgreSQL service > Variables tab

[database]
# Option 1: Full DATABASE_URL (recommended)
# Format: postgresql://username:password@host:port/database
DATABASE_URL = "postgresql://postgres:YOUR_PASSWORD@YOUR_HOST.railway.app:5432/railway"

# Option 2: Individual connection parameters (alternative)
# host = "YOUR_HOST.railway.app"
# port = 5432
# database = "railway"
# user = "postgres"
# password = "YOUR_PASSWORD"
